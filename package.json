{
  "name": "backend",
  "version": "1.0.0",
  "main": "index.js",
  "type": "module",
  "scripts": {
    "start": "node index.js",
    "postinstall": "pip install --break-system-packages -r requirements.txt || pip3 install --break-system-packages -r requirements.txt || python3 -m pip install --break-system-packages -r requirements.txt || python -m pip install --break-system-packages -r requirements.txt || true"
  },
  "dependencies": {
    "@supabase/supabase-js": "^2.0.0",
    "cors": "^2.8.5",
    "dotenv": "^16.0.0",
    "express": "^4.18.0",
    "multer": "^2.1.1",
    "openai": "^4.0.0",
    "pdf-parse": "^2.4.5",
    "xlsx": "^0.18.5"
  }
}
